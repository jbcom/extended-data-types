"""Formatting helper for CLI compatibility."""

from __future__ import annotations

from typing import Any


def format_output(data: Any) -> str:
    """Return stringified output."""
    return str(data)
